interface:
  display_name: "Outcome Integrity"
  short_description: "Preserve intent and advance focused verified work"
  default_prompt: "Use $outcome-integrity to preserve the full outcome, discern before persisting, return attention to the verified gap, and advance the next accountable authorized slice."
