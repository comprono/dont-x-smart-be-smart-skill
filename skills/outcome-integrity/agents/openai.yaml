interface:
  display_name: "Outcome Integrity"
  short_description: "Preserve intent and verify real project outcomes"
  default_prompt: "Answer the user's immediate question plainly first. Separate the user's outcome from methods and tooling layers, stop confusing reply loops, reconcile evidence, bound recurring side effects, and advance one verified slice."
