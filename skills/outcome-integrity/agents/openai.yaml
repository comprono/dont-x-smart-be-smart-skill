interface:
  display_name: "Outcome Integrity"
  short_description: "Preserve outcomes through causal proof"
  default_prompt: "Use $outcome-integrity to preserve the parented outcome stack and permanent capability floors, lock the earliest divergent transition, honor stop constraints, require production-shaped proof, and advance the next efficient authorized slice."
