interface:
  display_name: "Outcome Integrity"
  short_description: "Durable control without blocking delivery"
  default_prompt: "Use $outcome-integrity only if this work intrinsically needs durable tracking, recurring or unattended execution, or exact-once external-effect control. Keep ordinary local work on the direct lane, keep the control plane off the critical path, and advance the user's actual deliverable."
