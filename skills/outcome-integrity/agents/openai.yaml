interface:
  display_name: "Outcome Integrity"
  short_description: "Preserve intent and verify real project outcomes"
  default_prompt: "Separate the user's outcome from methods, reconcile evidence, bound recurring side effects, then advance one verified slice."
