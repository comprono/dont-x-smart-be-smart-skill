interface:
  display_name: "Outcome Integrity"
  short_description: "Preserve outcomes and proven capabilities"
  default_prompt: "Use $outcome-integrity to preserve the full parented outcome stack and permanent capability floors, keep fitness balanced, return attention to the verified gap, and advance the next efficient accountable authorized slice."
