interface:
  display_name: "Outcome Integrity"
  short_description: "Preserve intent and verify real project outcomes"
  default_prompt: "Separate the user's final outcome and proof from intermediate methods, reconcile project intent and acceptance state, then advance one evidence-backed slice."
