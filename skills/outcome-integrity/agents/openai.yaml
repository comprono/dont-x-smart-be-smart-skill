interface:
  display_name: "Outcome Integrity"
  short_description: "Preserve outcome hierarchy and verified progress"
  default_prompt: "Use $outcome-integrity to preserve the full parented outcome stack, return attention to the verified gap, prevent completion leakage, and advance the next accountable authorized slice."
